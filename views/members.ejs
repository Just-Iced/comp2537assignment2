<%- include("./partials/header", {pageTitle: "Members Only Page"})%>
<div class="container">
    <div class="col-md-12">
      <h1>Hello, <%= name %>!</h1>
      <div class="container-md">
        <div class="row row-cols-md-3 g-4">
          <% for (let i = 0; i < 3; i++) { %>
            <div class="col col-md-4">
              <img src=<%=`/images/${i}.jpg`;%> alt="Members Image" class="rounded img-fluid" />
            </div>
          <% } %>
        </div>
      </div>
      <p>Welcome to the members area.</p>
      <a href="/" class="btn btn-primary">Home</a>
  </div>

  <%- include("./partials/footer") %>